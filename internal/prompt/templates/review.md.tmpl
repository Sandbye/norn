---
{{- if .PR}}
pr: {{.PR.Number}}
title: {{printf "%q" .PR.Title}}
url: {{.PR.URL}}
{{- end}}
base: {{.Base}}
created: {{.Generated}}
---

# Review · {{if .PR}}PR #{{.PR.Number}} — {{.PR.Title}}{{else}}{{.Hint | default "PR review"}}{{end}}

This worktree is checked out to the PR branch. **Follow this repo's own conventions** (its `AGENTS.md` / `CLAUDE.md`, if present). Below is just the review context norn added.

{{if .PR}}## Under review

**PR #{{.PR.Number}} — {{.PR.Title}}**{{if .PR.URL}} · {{.PR.URL}}{{end}}

Review the diff of this PR against `{{.PR.Base}}`:

    git diff {{.PR.Base}}...HEAD
{{else}}## Review

{{.HintBlock}}

Read the diff against `{{.Base}}`.
{{end}}
Weigh: correctness, consistency with surrounding code, tests, security, completeness. Read surrounding code for context, don't review diffs in isolation. **Leave findings as review comments; change no code.**
